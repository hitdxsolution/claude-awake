# The release binaries ARE committed (see dist/) — they are ~2 MB each, so the whole set
# fits comfortably in git and users can install straight from a repo download.

.DS_Store
*.log

# 윈도우 리소스는 make 가 versioninfo.json 에서 만들어 낸다
*.syso
